import { AppConstants } from '@/app.constants'

export const playShadow = {
	shadowColor: AppConstants.primary,
	shadowOffset: {
		width: 0,
		height: 3
	},
	shadowOpacity: 0.6,
	shadowRadius: 8,
	elevation: 20
}
